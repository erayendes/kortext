node_modules
dist
ui/dist
package-lock.json
ui/package-lock.json

# Never edited again — formatting it would rewrite history for no reason.
archive

# Prose and tables are wrapped by hand here; a formatter would re-flow the
# columns and the line breaks that were chosen for reading, not for parsing.
*.md

# Citation metadata follows the CFF convention (and the other repos' style):
# double quotes, written by hand.
CITATION.cff

# The design system page is generated from the panel CSS, not hand-formatted.
dev/DESIGN.html
macos/build/
macos/Kortext.xcodeproj/

# Xcode writes the asset catalog manifests; xcodegen reads project.yml as is.
macos/Kortext/Assets.xcassets/
macos/project.yml
