KORTEXT_SIZE_LIMIT=500
KORTEXT_LOCK_TIMEOUT_SECONDS=300  # Saniye cinsinden lock timeout (Python script)
KORTEXT_LOCK_TIMEOUT_MIN=5         # Dakika cinsinden lock timeout (shell fallback)
KORTEXT_BACKUP_KEEP=5
KORTEXT_HOOK_MODE=permissive
KORTEXT_INTERACTION_LANGUAGE=tr    # tr | en
KORTEXT_STALE_HOURS_CONTEXT=24     # Aktif context dosyalarının stale eşiği (saat)
KORTEXT_STALE_HOURS_BACKLOG=48     # In Progress backlog item'larının stale eşiği (saat)
