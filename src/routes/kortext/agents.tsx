// Stub — filled by Session 5 (Settings / engine scope). See v6 implementation contract.
export function KortextAgentsRoute() {
  return (
    <section style={{ flex: 1, display: 'flex', flexDirection: 'column', minWidth: 0 }}>
      <div className="page-h">
        <span className="page-title">Agents — coming</span>
      </div>
    </section>
  );
}
