# Kortext v3.1.0 — UAT Test Rehberi (Eray)

> Bu dosya **Faz 12'nin** lokal kullanıcı doğrulamasıdır. PR #1 ([feat/v3.1-onboarding-and-dashboard-polish](https://github.com/erayendes/kortext/pull/1)) review/merge edilmeden önce VEYA merge sonrası npm publish öncesi koşulur.
> Hedef: yeni `.kortext/` mimari'sinin gerçek kurulumda çalıştığını + ekranların açıldığını + temel akışların kırılmadığını teyit etmek.
> Süre: ~30-45 dakika.

---

## 0. Ön hazırlık

Aşağıdaki şartların doğru olduğundan emin ol:

```bash
node --version        # v22 veya üstü (Kortext Node 22+ gerektiriyor)
git --version         # ≥ 2.30
which claude          # Claude CLI kurulu olmalı (executor seçimi için)
which agy             # AGY/Antigravity (opsiyonel)
which codex           # Codex CLI (opsiyonel)
```

Ayrıca **dashboard'da kullanılacak `KORTEXT_PORT=3200` portu boş olmalı**. Çakışırsa `kortext serve --port 3210` ile aç.

---

## 1. Build + pack (tgz üret)

PR henüz merge edilmediyse `feat/v3.1-onboarding-and-dashboard-polish` branch'ten çalış:

```bash
cd ~/Documents/_codebase/kortext
git checkout feat/v3.1-onboarding-and-dashboard-polish
git pull
npm install
npm run build         # dist/ üretir
npm pack              # kortext-3.0.0.tgz oluşur (version henüz 3.0.0)
ls -lh kortext-3.0.0.tgz
```

Çıktı: `kortext-3.0.0.tgz` ~5-10 MB.

> 💡 **Neden `3.0.0.tgz`?** `package.json`'da version hâlâ `3.0.0`. v3.1.0 release flow (3.1.0 bump + CHANGELOG + tag) **PR merge sonrası** yapılır. Bu UAT sırasında dosya adı önemli değil, içerik önemli.

---

## 2. Boş klasör + global install

Var olan eski kuruluma dokunmadan temiz bir klasörde test et:

```bash
# Eski global kortext'i kaldır
npm uninstall -g kortext

# Test klasörü
mkdir -p ~/Documents/_codebase/kortext-uat-v3.1
cd ~/Documents/_codebase/kortext-uat-v3.1
rm -rf .kortext agents workflows rules workspace AGENTS.md .env.example .gitignore  # temizle

# Lokal tgz'den global install
npm install -g ~/Documents/_codebase/kortext/kortext-3.0.0.tgz

# Versiyon teyit
kortext --version    # 3.0.0 yazmalı (package.json hâlâ 3.0.0)
kortext --help       # init / serve / start / approve / status / logs / cleanup / doctor / mcp / archive listelenmeli
```

`archive` subcommand'i v3.1 ile geldi (Faz 12.6). Listede görüyorsan **Faz 12 doğru paketlenmiş** demektir.

---

## 3. `kortext init` — yeni `.kortext/` layout

```bash
kortext init
```

Beklenen çıktı (örnek):
```
✓ Created AGENTS.md
✓ Created .gitignore
✓ Created .env.example
✓ Created .kortext/data/ (SQLite + log + worktree)
✓ Created .kortext/references/ (15 templates)
✓ Created .kortext/reports/ (11 templates)
✓ Created .kortext/memory/ (handover.md + decisions.md + learned.md)
Initialized Kortext project at /Users/erayendes/Documents/_codebase/kortext-uat-v3.1
```

**Doğrulama (kritik):**

```bash
ls -a
# Beklenen: . .. .gitignore .env.example .kortext AGENTS.md
# Eski v3.0'da olan ama v3.1'de OLMAMASI gereken: agents/ workflows/ rules/ workspace/

ls .kortext/
# Beklenen: data references reports memory

ls .kortext/references/ | wc -l       # 15 olmalı
ls .kortext/reports/ | wc -l          # 11 olmalı
ls .kortext/memory/                    # decisions.md  handover.md  learned.md
ls .kortext/data/                      # kortext.db (init migrate koştu)
```

✅ Pass: proje kökünde `agents/workflows/rules/workspace/` **YOK**, `.kortext/` altında templates kopyalandı.
❌ Fail: `agents/` proje kökünde varsa Faz 12.2 yanlış paketlendi.

Frontmatter check:
```bash
head -8 .kortext/references/blueprint.md
# Beklenen:
# ---
# status: uninitialized
# author: +product-manager  (veya benzeri)
# approver: +prime
# ---
```

INFO callout sökülmüş olmalı — `grep "INFO" .kortext/references/*.md` boş dönmeli.

---

## 4. `kortext serve` — dashboard

```bash
kortext serve
```

Beklenen çıktı:
```
[engine] Boot: syncing personas + workflows into SQL index...
[engine] ✓ 14 personas + N workflow_steps indexed
[server] Listening on http://localhost:3200
[browser] Opening http://localhost:3200 ...
```

⚠️ **Eğer boot'ta `fatal: workflow references unknown persona +X` hatası alıyorsan**: Faz 13 (workflow content rewrite) yapılmadığı için workflow .md'lerinde `+ajan` placeholder'ı veya geçersiz handle var. Bu **beklenen davranış** — Faz 12.8'in FK validation'ı koruma katmanı. Faz 13 bunu temizleyecek.

Geçici workaround (sadece UAT için, üretimde değil): `kortext serve --skip-index-validation` flag'i şu an yok; bu UAT'da pipeline tetiklenmesini test edemiyorsan dert etme — dashboard ekranları açılıyorsa yeterli.

**Tarayıcı kontrolü** — http://localhost:3200 açıldığında:

- [ ] Sidebar görünüyor (Dashboard / Board / Memory / Reports / References + Settings sub-panes)
- [ ] Onboarding ekranı geliyor (`.kortext/references/blueprint.md` status `uninitialized` olduğu için)

---

## 5. Onboarding wizard

Onboarding ekranında:

1. **Project Name:** `Acme CRM UAT v3.1`
2. **Project Code:** `ACMEV31`
3. **Project Type:** `existing` veya `new`
4. **Target Platform:** Web (chip)
5. **Executor:** Mock seç (Claude/AGY testi için sonra)
6. **Blueprint:** `~/Documents/_docbase/kortext/docs/blueprint-helsinki.md` (eskiden kullanılan örnek) **veya** Eray'ın yazdığı yeni bir blueprint
7. **Submit**

Beklenen:
- [ ] Form yeşil geçer, `Initializing…` durduktan sonra dashboard'a yönlendirilir
- [ ] `.kortext/references/blueprint.md` status'u `approved` olur (`head -8` ile teyit)
- [ ] `.kortext/data/kortext.db`'de yeni `run` satırı oluşur (mock executor 01a-analysis-pipeline tetikler)
- [ ] Dashboard'da "Active work" tablosu yeni run'ı gösterir

⚠️ **Mock executor sahte success döner — gerçek dosya yazılmaz.** Bu UAT için yeterli; gerçek pipeline akışı Faz 13 sonrası test edilir.

---

## 6. Board ekranı "+ New Item" modal

Sidebar → Board.

- [ ] "Filter" + "+ New task" butonları görünür
- [ ] "+ New task" tıklayınca modal açılır
- [ ] Modal alanları: Type (dropdown) / Title (required) / Epic (dropdown) / Owner (persona) / Acceptance criteria / Dependencies / Notes
- [ ] Type=task, Title="UAT test login flow", Submit
- [ ] Modal kapanır, board'da `T01 — UAT test login flow` görünür

Sonra DB direkt teyit:
```bash
sqlite3 ~/Documents/_codebase/kortext-uat-v3.1/.kortext/data/kortext.db \
  "SELECT id, type, title, status FROM backlog_items;"
# T01 | task | UAT test login flow | to_do
```

---

## 7. Settings readonly editor

Sidebar → Settings → Agents.

- [ ] Sol kolonda persona listesi (14 entry)
- [ ] Persona seçilince sağda markdown render gözüküyor
- [ ] "Edit" / "Save" butonları **YOK** — bunun yerine "Source / Rendered" toggle var
- [ ] Toggle: Rendered = HTML rendered markdown; Source = raw .md text

Workflows ve Rules pane'lerinde aynı pattern.

---

## 8. Prompt cache token usage delta (Faz 12.7)

Üç defa aynı pipeline'ı koş (mock değil — Claude executor ile):

```bash
# Onboarding'i Claude executor ile yeniden yap, ya da:
kortext start 01a-analysis-pipeline --executor claude
# Log dosyasında token usage'ı oku:
tail -20 .kortext/data/logs/run-*-step-*.log | grep -i "token"
```

Üç koşumun token kullanımı:
- **İlk koş:** baseline (cache miss)
- **2. koş:** input tokens ~%90 azalmış olmalı (cache hit)
- **3. koş:** 2. ile benzer

Eğer fark yoksa: `claude --print` çıktısında `cache_read_input_tokens` field'ına bak — 0'dan büyükse cache çalışıyor demektir.

⚠️ Bu test gerçek Claude API çağrısı yapıyor — kredi yakıyor. 3× küçük task ile test et.

---

## 9. Handover rotation (Faz 12.6)

Manuel test:

```bash
# 6 fake entry yaz handover.md'ye
for i in $(seq 1 6); do
  cat >> .kortext/memory/handover.md <<EOF

---
status: approved
author: +product-manager
updated_at: $(date -u +%Y-%m-%dT%H:%M:%SZ)
---

## Handover: UAT test entry $i

Test content $i

EOF
done

kortext archive handover
ls .kortext/memory/
# Beklenen: handover.md (boş) + handover-2026-05-25-XXXX.md (rotated)
```

✅ Pass: rotation çalıştı, archive dosyası timestamp suffix'iyle yazıldı.

---

## 10. Cleanup

UAT bitince:
```bash
cd ~
rm -rf ~/Documents/_codebase/kortext-uat-v3.1
npm uninstall -g kortext
```

---

## Sonuç raporlama

Hangi adımda pass / fail olduğunu HANDOVER-v3.md'ye yaz (Faz 12 sonu bölümüne). Fail varsa: hangi adım, hangi log, hangi screenshot. Sonraki oturum açar açmaz fix turuna geçilir.

✅ Hepsi pass: PR merge edilebilir → v3.1.0 release flow başlar:
1. PR merge → main
2. `package.json` 3.0.0 → 3.1.0
3. `CHANGELOG.md` `## [3.1.0]` bölümü ekle
4. `git tag v3.1.0 && git push origin v3.1.0` → npm-publish.yml otomatik tetikler
5. npm registry'de `kortext@3.1.0` yayınlanır (provenance-attested)

---

## Notlar

- **Faz 13 (workflow content rewrite)** bu UAT'tan bağımsız — yapılana kadar gerçek pipeline akışı (mock değil) `+ajan` placeholder hataları verebilir. UAT bunu engellemez; dashboard + init + frontmatter + UI testleri gerçek pipeline gerektirmez.
- **Faz 12.9 yarım UI** (Reports SQL revamp + Memory archive dropdown) eksik ama mevcut ekranlar v3.0 endpoint'leriyle çalışır — UAT'da Reports/Memory ekranları açılıyor mu kontrol et, "tıkırında" olmayabilir.
- **`app.listen` EADDRINUSE silent fail** hâlâ açık (Faz 10 borcu). Eğer `kortext serve` çıktısı boş geliyorsa port çakışmasıdır — `lsof -ti:3200 | xargs kill` ile temizle.
